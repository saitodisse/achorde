import type { Preview } from "@storybook/react-vite";
import "../src/components/InteractiveFretboard/interactive-fretboard.css";

const preview: Preview = {
	parameters: {
		layout: "centered",
	},
};

export default preview;
