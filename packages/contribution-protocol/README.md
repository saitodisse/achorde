# @achorde/contribution-protocol

Browser-safe, forge-neutral contracts for one-file artist portal contributions. It owns manifest validation, canonical JSON and content hashing. It deliberately does not contain React, ZIP IO, OAuth, persistence, Git, or forge SDKs.
