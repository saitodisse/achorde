# @achorde/contribution-protocol

Browser-safe, forge-neutral contracts for artist portal contributions. It preserves the `achorde.portal-contribution/v1` parser and adds `v2` manifests for atomic artist/work/chart updates, rights attestations, path allowlists, deterministic entry ordering, and size limits. It deliberately does not contain React, ZIP IO, OAuth, persistence, Git, or forge SDKs.
