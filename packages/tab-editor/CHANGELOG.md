# Changelog

## 0.1.0

- Add headless chord chart text analysis and text-change proposal helpers.
- Add `ChordChartEditor` React component with lazy Monaco loading, textarea
  fallback, diagnostics, save payloads, and `@achorde/tab-renderer` preview.
- Add importable editor stylesheet at `@achorde/tab-editor/style.css`.
